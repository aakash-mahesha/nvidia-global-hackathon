"""evtol — eVTOL Charging (arm) hackathon package.

Skeleton modules:
    config         — typed settings loaded from .env (python-dotenv)
    token_factory  — OpenAI-compatible client + PermitDecision schema
    topics         — the five event-bus topic constants
"""

__version__ = "0.1.0"
